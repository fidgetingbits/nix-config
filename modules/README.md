Everything in `modules/` is auto-loaded.

Any modules in `auto/` folders are automatically enabled by default,
or based on other optional settings.

Anything else is enabled only if toggled via `enable` flag.
