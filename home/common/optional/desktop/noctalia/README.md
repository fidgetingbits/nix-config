# Noctalia

There are just recipes available for auto saving updated configurations. See `just noctalia-save` and `noctalia-save-host` for examples.

There is also a timer that snapshots noctalia settings changes in case you forget to save any before reboot, etc. Snapshots are saved in `~/.cache/noctalia/backup/`.
